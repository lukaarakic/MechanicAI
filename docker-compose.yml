version: '3.8'

services:
  mechanicai:
    build: .
    ports:
      - '3000:3000'
    restart: always
    env_file:
      - .env
    volumes:
      - mechanicai-data:/app/prisma

volumes:
  mechanicai-data:
